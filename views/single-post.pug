extends page-outline.pug

block content
  div.single-post-container

    div.single-name
      p= whiskeyName

    div.single-photo
      img(src=smallImageURL)

    div.single-rating
      p= 'Rating: ' + rating

    div.single-comment
      h2 Comments:
      each whatever in comment
        p= whatever.text
        p= whatever.commentDate.toLocaleDateString('en-US')

    div.single-add-comment
      form(method='POST' enctype='application/json' action="/single-post/" + _id)
        textarea#add-comment(name="text", placeholder='Comment on this whiskey')
        input(type='submit', value='Submit')

    div.single-delete-post
      p Delete this post?
      form(action="/single-post/" + _id + "?_method=DELETE" method="POST")
        //- a(href="/user/" + user)
        input(type='submit', value='Delete')
